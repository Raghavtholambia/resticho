<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Premium Listings</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Fonts & Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet" />

    <style>
   

      .listing-col {
        padding: 10px;
      }

      .listing-card {
        border-radius: 18px;
        overflow: hidden;
        transition: all 0.25s ease;
        background: #fff;
        cursor: pointer;
      }

      .listing-card:hover {
        transform: scale(1.03);
      }

      .listing-img {
        height: 180px;
        position: relative;
        border-radius: 18px;
        overflow: hidden;
      }

      .listing-img img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: 0.4s ease;
        filter: brightness(100%);
      }

      .listing-card:hover img {
        transform: scale(1.08);
      }

      /* ADMIN BANNER */
      .not-verified-banner {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.65);
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        font-size: 0.95rem;
        font-weight: 600;
        color: #fff;
        padding: 10px;
      }

      .guest-fav {
        position: absolute;
        top: 12px;
        left: 12px;
        background: #ffffff;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
        box-shadow: 0px 2px 8px rgba(0,0,0,0.15);
      }

      .wish-icon {
        position: absolute;
        top: 10px;
        right: 10px;
        background: #ffffff;
        width: 34px;
        height: 34px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
        box-shadow: 0px 2px 8px rgba(0,0,0,0.20);
      }

      .card-body {
        padding: 8px 2px 0px 2px;
      }

      .place-title {
        font-size: 0.92rem;
        font-weight: 600;
        margin-bottom: 2px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        color: #212529;
      }

      .price-text {
        font-size: 0.85rem;
        color: #444;
      }

      .rating-text {
        font-size: 0.82rem;
        color: #222;
        font-weight: 500;
      }
    </style>
  </head>

  <body>
    <div class="container py-4">

      <div class="row">
        <% listings.forEach(listing => { %>
          <div class="col-6 col-md-4 col-lg-2 listing-col">

            <% if(listing.verifiedByAdmin) { %>
            <a href="/listing/<%= listing.id %>" class="text-decoration-none text-dark">
                          <% } %>

              <div class="listing-card">

                <!-- IMAGE SECTION -->
                <div class="listing-img">

                  <img src="<%= listing.image.url %>" alt="Listing Image" />

                  <!-- NOT VERIFIED BANNER -->
                  <% if (!listing.verifiedByAdmin) { %>
                    <div class="not-verified-banner">
                      Not Available Right Now
                    </div>
                  <% } %>

                  <!-- Guest favourite -->
                  <div class="guest-fav">Guest favourite</div>

                  <!-- Wishlist -->
                  <div class="wish-icon">
                    <i class="fa-regular fa-heart"></i>
                  </div>
                </div>

                <!-- DETAILS -->
                <div class="card-body">
                  <div class="place-title"><%= listing.itemName %></div>

                  <div class="price-text">
                    <% var p = listing.pricing || {}; %>
                    <% if (listing.businessMode === 'rental' || listing.businessMode === 'both') { %>
                      ₹<%= (p.rentalPricePerDay || 0).toLocaleString('en-IN') %>/day
                    <% } %>
                    <% if (listing.businessMode === 'both') { %> · <% } %>
                    <% if (listing.businessMode === 'custom' || listing.businessMode === 'both') { %>
                      ₹<%= (p.stitchingBasePrice || 0).toLocaleString('en-IN') %> stitch
                    <% } %>
                  </div>

                  <div class="rating-text">
                    <% if (listing.averageRating && listing.averageRating > 0) { %>
                      ★ <%= listing.averageRating.toFixed(2) %>
                    <% } else { %>
                      ★ New
                    <% } %>
                  </div>
                </div>

              </div>
            </a>

          </div>
        <% }) %>
      </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

